#check the signature :
diff signature.txt <(echo "a608ed62f69fec4cac1a9636f2edd281125a8928")

#use ssh
ssh malhendi@localhost -p 4242

#see its debian :
hostnamectl

#show all groups :
sudo groups

#add user :
sudo adduser username

#add group :
sudo addgroup groupname

#add user to group :
sudo adduser username groupName

#check if user in group :
getent group [nameOfGroup]

#check the ufw :
sudo service ufw status
sudo systemctl status ufw 

#check the ssh :
sudo service ssh status
sudo systemctl ssh status 

#check how we made the ssh only acsess at the 4242 port :
sudo nano etc/ssh/sshd_config

#check password policy:
/etc/pam.d/common-password

#check password age :
sudo nano /etc/login.defs

#change hostname :
sudo hostnamectl set-hostname [new_hostname]

#Partition :
lsblk
df -h

#sudo password :
nano /etc/sudoers.d/sudo_config

#we made :
mkdir /var/log/sudo

Check the days : 
sudo chage -l [username]

Change the orders of the days :
sudo chage -m 2 -M 30 [username]

Make sure it is downloaded to your device:
dpkg -l | grep [name]  || [name] -V
ex:dpkg -l | grep sudo
ex:sudo -v

#Add a new rule to open port 8080 :
ufw allow 8080

#delete  rule 8080:
ufw delete allow 8080

#port number allow :
sudo ufw allow 22    # Allow SSH on default port 22
sudo ufw allow 4242  # Allow SSH on custom port 4242 (like in Born2beRoot)
sudo ufw allow 80    # Allow HTTP (port 80)
sudo ufw allow 443   # Allow HTTPS (port 443)
sudo ufw deny 23     # blocks Telnet # Deny a port

#open cron
sudo crontab -u root -e

# New way ssh (recommended) :
sudo systemctl status ssh
sudo systemctl start ssh
sudo systemctl stop ssh
sudo systemctl restart ssh    # Restart the service
sudo systemctl enable ssh     # Enable auto start at boot
sudo systemctl disable ssh    # Disable auto start at boot

# In UFW also you can use :
sudo ufw allow ssh
sudo ufw allow http   
sudo ufw allow https    

mariadb -u malhendi -p
SHOW DATABASES;

sudo systemctl status lighttpd mariadb

sudo systemctl status lighttpd --no-pager

# install the server (you only have the client now)
sudo apt update
sudo apt install mariadb-server

# start & enable the service
sudo systemctl enable --now mariadb

# run the hardening script
sudo mariadb-secure-installation


Follow the prompts: set root auth as you prefer, remove anonymous users, disallow remote root, remove test DB, reload privileges.

# is the service running?
systemctl status mariadb

# can you log in as root (socket auth)?
sudo mariadb

If you actually want Oracle MySQL (not MariaDB)

Install mysql-server instead:

sudo apt update
sudo apt install mysql-server
sudo systemctl enable --now mysql
sudo mysql_secure_installation

sudo apt install glances -y
glances

Pick one stack and stick with it to avoid tool/name mismatches.
